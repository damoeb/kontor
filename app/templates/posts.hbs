{{#side-nav onOpenModal=(action 'openModal') }}
  {{#post-create onsave=(action 'savePost')}}

  {{/post-create}}
{{/side-nav}}
<div class="main-container">
    <ul>
      {{#each model.posts as |post|}}
          <li class="row">
              <div><a href="{{post.url}}">{{post.title}}</a></div>
              <div>{{post.description}}</div>

              <div>
                  <div class="col s8">
                    {{#each post.tags as |tag|}}
                        <a href="#/tags/{{tag}}">
                            <span class="tag">{{tag}}</span>
                        </a>
                    {{/each}}
                  </div>
                  <div class="col s4 pull-right">
                      <a href="javascript:void(0)" {{action 'edit' post}}>edit</a>
                      <a href="javascript:void(0)" {{action 'remove' post}}>remove</a>
                  </div>
              </div>

          </li>
      {{/each}}
    </ul>
</div>